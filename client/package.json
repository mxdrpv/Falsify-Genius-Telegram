{
  "name": "falsify-genius-telegram",
  "type": "module",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "telegraf": "^4.12.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}
